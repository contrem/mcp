from setuptools import setup, find_packages

setup(
  name='mcp',
  author='Michael Contreras',
  version='0.0.1',
  packages=['mcp'],
  package_dir={'mcp': 'mcp'},
  install_requires=[
  ],
  scripts=[
  ],
  include_package_data=True,
)
